# 未训练模型（仅供集成测试）

这里的 .onnx 是用**随机初始化权重**导出的，增强效果为负，绝不能当成可用模型。

存在的意义：在 Colab 训练完成之前，就把"导出 → 加载 → 接入 Pipeline → 延迟计量"
这条工程链路验证通过。训练几小时后才发现导不出来是最糟的情况。

放在子目录里是刻意的：`discover_onnx_models()` 只扫描 `models/*.onnx` 顶层，
所以这些文件不会出现在 Web 界面的方法列表里。

Colab 训练产出的真模型请放到上一级目录 `models/`。
