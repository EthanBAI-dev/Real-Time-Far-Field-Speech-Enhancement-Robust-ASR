# 指标定义与结果

> **2026-08-18 V1 方向校正**：结果拆为 DNS客观质量、AISHELL受控中文CER、
> WenetSpeech真实会议CER三张表。旧单测试集及其指标文件已清理，不再混入本页。
>
> 本文档的**指标定义、测量方法学、复杂度/实时性数字**保留——
> 那些只跟代码和硬件有关，跟用哪份数据无关。

---

## 1. 指标定义与现状

| 指标 | 含义 | 方向 | 现状 |
|---|---|---|---|
| **SI-SDR** | 尺度不变信噪失真比 | ↑ | ✅ 自研实现 |
| SDR | 普通信噪失真比（不做尺度不变） | ↑ | ✅ 自研实现 |
| SegSNR | 分段信噪比，只统计有语音的帧 | ↑ | ✅ 自研实现 |
| **STOI / ESTOI** | 短时客观可懂度 | ↑ | ✅ pystoi |
| PESQ-WB | ITU-T P.862.2 感知质量 | ↑ | ⏸ **仅 Colab**（Windows 无 wheel，见 I-05） |
| DNSMOS SIG/BAK/OVRL | 无参考 MOS 预测 | ↑ | ⏸ 模型可下载，打分函数未实现 |
| **CER** | ASR 字错率（中文按字算） | ↓ | ✅ `rtse-eval` 已实现（faster-whisper） |
| WER | 词错率 | ↓ | ✅ 已实现，但**对中文无意义**（无空格，整句被当一个词） |
| **RT60 实测** | Schroeder 反向积分估计混响时间 | — | ✅ 自研实现（`rtse.dsp.rt60`） |
| **噪声平稳性** | 去趋势帧能量动态范围 | — | ✅ 自研实现（`rtse.dsp.stationarity`） |
| 参数量 / 模型大小 | 复杂度 | ↓ | ✅ 已测 |
| **RTF** | 处理耗时 ÷ 音频时长（CPU 单线程） | ↓ | ✅ 已测 |
| 处理延迟 p50/p95/p99 | 每帧计算耗时分位数 | ↓ | ✅ 已测 |
| 算法延迟 | STFT 窗长决定的固有延迟 | ↓ | ✅ 32 ms（固定） |

### 为什么 CER 是第一指标，而不是 SI-SDR

语音质量指标与可懂度**经常方向相反**：单通道降噪能显著改善信噪比与听感舒适度，
但几乎不改善、甚至损害可懂度——增益函数在压制噪声的同时不可避免地衰减了
与噪声谱重叠的弱语音成分（尤其是能量低但对区分音素至关重要的辅音）。
SI-SDR 只看能量比，对"少了一个辅音"几乎无感。

所以"SI-SDR 涨了 5 dB"这句话对 ASR 是不是好事，**必须直接测 CER 才知道**。
指标表专门设 ΔSI-SDR 与 ΔCER 两列并列，展示两者的相关性有多弱——
这比单报一个好看的 CER 更有说服力。

### 为什么判断实时性要看 p99 而不是 RTF 均值

RTF 均值只有 1 ms、但 p99 冲到 20 ms 的系统，听感上是每隔几秒爆一次音。
**帧预算是 16 ms**（一个帧移），p99 越过它，缓冲就会持续累积、延迟无界增长。
所以 `LatencyStats.realtime_ok` 的判据是 p99 < 帧预算，不是 RTF < 1。

---

## 2. 已实测：复杂度与实时性

> 这一节的数字**与数据集无关**（只取决于模型结构和本机硬件），换数据集后依然有效。

**测量条件**：Intel i7-14700，**CPU 单线程**（`intra_op_num_threads=1`），
16 kHz / n_fft 512 / hop 256，2026-08-05

### 2.1 模型复杂度

| 模型 | 参数量 | ONNX 大小 | 编码器通道 | GRU | 时间感受野 |
|---|---|---|---|---|---|
| crn-nano | 118,370 | 475 KB | 8-16-16-24-32 | 1×64 | 6 帧 |
| crn-lite | 569,282 | 2,237 KB | 16-32-32-48-64 | 2×128 | 6 帧 |
| crn-large | 1,770,722 | 6.8 MB | 24-48-64-64-96 | 2×256 | 6 帧 |

频率维逐层：257 → 129 → 65 → 33 → 17 → 9。三档模型**全部严格因果**（已由测试强制）。

### 2.2 单帧处理耗时 · PyTorch vs ONNX Runtime

| 模型 | PyTorch (ms) | **ONNX (ms)** | 加速比 | ONNX RTF |
|---|---|---|---|---|
| crn-nano | 0.520 | **0.104** | 5.0× | 0.0065 |
| crn-lite | 0.666 | **0.382** | 1.7× | 0.0239 |

**ONNX Runtime 明显快于 PyTorch**，小模型上尤其显著（5 倍）——
PyTorch 每层的 Python 调度开销在单帧粒度下占比很高，而 ONNX 图做了算子融合和内存复用。
这直接支持了"导出 ONNX 而不是直接部署 PyTorch"这个决定。

### 2.3 完整管线延迟（VAD + 增强 + STFT/iSTFT）

| 方法 | RTF | p50 (ms) | p95 (ms) | p99 (ms) | 帧预算 | 余量 |
|---|---|---|---|---|---|---|
| specsub | 0.0064 | — | — | 0.53 | 16 ms | 30× |
| wiener | 0.0058 | — | — | 0.42 | 16 ms | 38× |
| mmse-lsa | 0.0103 | — | — | 0.55 | 16 ms | 29× |
| crn-nano (ONNX) | 0.0226 | 0.331 | 0.498 | 0.627 | 16 ms | 26× |
| crn-lite (ONNX) | 0.0344 | 0.516 | 0.753 | 1.024 | 16 ms | 16× |

**全部方法的实时性都不是瓶颈** —— 即便最重的 crn-lite 也有 16 倍余量。
这说明在本项目的规模下，**质量才是唯一需要权衡的维度**，不必为了算力牺牲效果。

MMSE-LSA 比维纳慢 60%，代价来自 `scipy.special.exp1`（指数积分）——
这是它用算力换听感的地方，如实体现在表里。

### 2.4 延迟构成

| 组成 | 值 | 能否消除 |
|---|---|---|
| **算法延迟**（STFT 窗长） | **32 ms** | ❌ 方法固有，CPU 再快也消不掉 |
| 处理延迟 p99 | 0.4 ~ 1.0 ms | ✅ 可优化，但目前完全不是瓶颈 |
| 帧移（缓冲粒度） | 16 ms | 可调，但会改变频率分辨率 |
| WebSocket 往返 + 浏览器缓冲 | 待测 | 仅 Web 演示路径有 |

---

## 3. V1 冒烟结果（Nano 3 epoch，仅验证闭环）

> ⚠️ **已被第 4 节取代**（2026-08-19 正式训练 25 epoch）。
> 保留本节是因为它是「训练分布缺高 SNR 端」那个 bug 的现场证据，
> 与第 4 节对照能看出修复前后的差距。**不要引用本节数字作为成绩。**

> 规模：DNS 81条、AISHELL 81条、WenetSpeech 30条；ASR 为
> faster-whisper `small`。该模型只训练3 epoch，结果用于决定能否进入正式训练，
> **不作为最终模型成绩**。

### 3.1 DNS 客观质量

| 方法 | SI-SDR | ΔSI-SDR | STOI | ESTOI | PESQ（Colab） |
|---|---:|---:|---:|---:|---:|
| noisy | 5.91 | — | 0.791 | 0.657 | 1.526 |
| specsub | 5.27 | −0.64 | 0.783 | 0.644 | 1.515 |
| wiener | 4.83 | −1.09 | 0.765 | 0.630 | 1.499 |
| mmse-lsa | 4.91 | −1.00 | 0.766 | 0.631 | 1.522 |
| crn-nano（3 epoch） | −0.37 | **−6.28** | 0.718 | 0.558 | 1.415 |

### 3.2 AISHELL-1 受控中文 CER

| 输入 | CER | 相对 noisy 的 ΔCER |
|---|---:|---:|
| clean 上界 | 0.093 | — |
| noisy | 0.366 | — |
| specsub | 0.458 | +0.092 |
| wiener | 0.421 | +0.055 |
| mmse-lsa | 0.460 | +0.094 |
| crn-nano（3 epoch） | 0.462 | **+0.096** |

三种DSP和Nano的配对ΔCER 95% bootstrap CI下界都大于0：谱减
[+0.021,+0.198]、维纳[+0.025,+0.085]、MMSE-LSA[+0.035,+0.190]、Nano
[+0.064,+0.132]。在这套受控中文数据上，退化方向明确。

### 3.3 WenetSpeech 真实会议 CER

| 输入 | CER | 相对原始输入的 ΔCER |
|---|---:|---:|
| 原始会议输入 | 0.199 | — |
| specsub | 0.210 | +0.010 |
| wiener | 0.199 | +0.000 |
| mmse-lsa | 0.193 | −0.006 |
| crn-nano（3 epoch） | 0.237 | +0.037 |

三种DSP的配对区间均跨0：谱减[−0.025,+0.046]、维纳[−0.031,+0.028]、
MMSE-LSA[−0.035,+0.019]。MMSE-LSA的−0.006只是方向性小幅改善，30条样本
不足以宣称有效。Nano区间为[−0.003,+0.079]，同样不能宣称显著退化。

### 3.4 冒烟结论

数据下载、三套索引、SNR校准、ONNX流式推理和CER评测闭环全部跑通；但3-epoch
Nano没有通过质量门槛。下一步应继续正式训练并使用训练闸门筛选，不能把这份模型
包装成可用的降噪结果。

## 4. V1 结果表 · crn-nano 正式训练 25 epoch（2026-08-19）

> 模型：`checkpoints/crn-nano/best.pt`，25 epoch，验证 SI-SDR 19.93 dB。
> 导出流式 ONNX 通过三层一致性校验（最大误差 2.3e-06）。
> 规模仍是冒烟档（81/81/30 条），**逐样本配对 + bootstrap 95% CI**。
> lite / large 尚未在新配方上训练。

### 4.0 训练闸门

| 闸门 | 3 epoch 冒烟 | **25 epoch** | 要求 | 结果 |
|---|---:|---:|---|---|
| 干净输入透传 SI-SDR（DNS，n=81 中位） | 8.18 | **37.81** | ≥ 20 | ✅ |
| 留出混音 ΔSI-SDR（DNS） | −0.89 | **+2.82** | > 0 | ✅ |

同一道透传检验换到 AISHELL 干净参考上只有 **16.17 dB**——
无害性没有跨域迁移，详见 [`FINDINGS.md`](FINDINGS.md) F-15④。

### 4.1 DNS 客观质量（81 条，训练域内）

| 方法 | SI-SDR | ΔSI-SDR | 95% CI | STOI | ESTOI |
|---|---:|---:|---|---:|---:|
| none | 5.91 | — | — | 0.791 | 0.657 |
| specsub | 5.27 | +0.42 | [+0.10,+0.77] ✅ | 0.783 | 0.644 |
| wiener | 5.32 | +0.39 | [+0.09,+0.71] ✅ | 0.789 | 0.650 |
| mmse-lsa | 5.32 | +0.39 | [+0.12,+0.67] ✅ | 0.789 | 0.650 |
| **crn-nano** | **7.78** | **+2.82** | **[+2.04,+3.61] ✅** | 0.792 | 0.653 |

> 表中 SI-SDR 列是**全集均值**（含 identity 格），ΔSI-SDR 列是
> **逐样本配对差的均值**（不含 identity 格）——两者口径不同，不能相减对上。

按噪声平稳性拆（ΔSI-SDR 均值）：

| 方法 | 非稳态 | 稳态 |
|---|---:|---:|
| specsub | **−0.29** | +1.13 |
| wiener | −0.25 | +1.03 |
| mmse-lsa | −0.21 | +0.98 |
| **crn-nano** | **+3.38** | +2.27 |

按 SNR 拆（ΔSI-SDR 均值）：

| 方法 | −5 | 0 | 5 | 10 | 15 |
|---|---:|---:|---:|---:|---:|
| specsub | +0.21 | +0.82 | +0.85 | +0.03 | +0.20 |
| crn-nano | +4.05 | +3.67 | +2.98 | +2.32 | +1.10 |

### 4.2 AISHELL-1 受控中文（81 条，域外）

| 方法 | CER | ΔCER | 95% CI | ΔSI-SDR | 95% CI |
|---|---:|---:|---|---:|---|
| clean 上界 | 0.088 | — | — | — | — |
| none | 0.379 | — | — | — | — |
| specsub | 0.415 | +0.037 | [+0.005,+0.071] ❌ | +0.74 | [+0.23,+1.28] |
| wiener | 0.423 | +0.045 | [+0.007,+0.085] ❌ | +0.63 | [+0.24,+1.03] |
| mmse-lsa | 0.407 | +0.029 | [−0.010,+0.069] — | +0.59 | [+0.24,+0.94] |
| **crn-nano** | **0.531** | **+0.154** | **[+0.099,+0.211] ❌** | +0.34 | [−1.22,+1.86] — |

**这张表是 M-01 最直接的证据**：三种 DSP 的 ΔSI-SDR 全部显著为正，
ΔCER 却全部为正（更差）。声学指标与下游任务在这里是**反向**的。

按 SNR 拆（ΔCER 均值）：

| 方法 | −5 | 0 | 5 | 10 | 15 |
|---|---:|---:|---:|---:|---:|
| specsub | +0.082 | +0.001 | +0.045 | +0.051 | +0.007 |
| **crn-nano** | +0.091 | +0.184 | +0.219 | **+0.250** | +0.028 |

### 4.3 WenetSpeech 真实会议（30 条，无干净参考）

| 方法 | CER | ΔCER | 95% CI |
|---|---:|---:|---|
| 原始输入 | 0.199 | — | — |
| specsub | 0.210 | +0.010 | [−0.026,+0.046] — |
| wiener | 0.190 | −0.010 | [−0.037,+0.014] — |
| mmse-lsa | 0.188 | −0.011 | [−0.038,+0.012] — |
| **crn-nano** | **0.301** | **+0.101** | **[+0.033,+0.169] ❌** |

**三种 DSP 在真实会议上无害**（区间全部跨零）；**只有神经网络显著变差**。

### 4.4 待补

| 项 | 状态 |
|---|---|
| PESQ / DNSMOS | ⏸ 仅 Colab / 打分函数未实现 |
| crn-lite / crn-large 新配方重训 | ⏸ 未做——"模型越大越差"因此**无法验证** |
| 正式规模（405/405/300 条） | ⏸ 当前仍是冒烟档 |
| `medium` ASR 复核 | ⏸ 只跑了 `small` |

## 5. 测量方法学

为了让数字可信、可复现：

1. **指标一律在内存数组上计算，绝不写盘再读回**。
   `write_audio` 的削波保护会静默改变幅度，污染 SDR、SegSNR 这类非尺度不变指标（I-07）。
2. **RTF 与延迟在 CPU 单线程下测**。多线程能让数字好看，但掩盖真实算力需求，
   也无法反映多路并发时的表现。
3. **离线评测走的就是实时路径**。`StreamingEnhancer.process()` 内部逐帧调用
   `process_frame`，与 Web 演示、与 C++ 版是同一条代码路径。
   离线数字**就是**部署时的数字，不存在"论文好、上线崩"。
4. **NN 与 DSP 走完全相同的管线**。`OnnxEnhancer` 包装成 `StreamingEnhancer`，
   同一条 `Pipeline`、同样的 STFT、同样的计时点 —— 对比才是公平的。
5. **看分位数不看均值**。实时系统的失败由尾部决定。
6. **标称参数与实测值分开记录**。测试集里 RT60 同时记 `rt60_nominal` 和
   `rt60_measured`——踩过一次坑（I-22：标称 0.9 s 实测只有 0.63 s），
   此后一律以实测为准。
