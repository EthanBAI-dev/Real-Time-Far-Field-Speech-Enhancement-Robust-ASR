# 指标定义与结果

> 已实测的数字标注了测量条件；等 Colab 产出的位置明确标 **待填**，不放占位数字。

---

## 1. 指标定义与现状

| 指标 | 含义 | 方向 | 现状 |
|---|---|---|---|
| **SI-SDR** | 尺度不变信噪失真比 | ↑ | ✅ 自研实现 |
| SDR | 普通信噪失真比（不做尺度不变） | ↑ | ✅ 自研实现 |
| SegSNR | 分段信噪比，只统计有语音的帧 | ↑ | ✅ 自研实现 |
| **STOI / ESTOI** | 短时客观可懂度 | ↑ | ✅ pystoi |
| PESQ-WB | ITU-T P.862.2 感知质量 | ↑ | ⏸ **仅 Colab**（Windows 无 wheel，见 I-05） |
| DNSMOS SIG/BAK/OVRL | 无参考 MOS 预测 | ↑ | ⏸ 待下载 ONNX 模型 |
| **CER / WER** | ASR 字/词错率 | ↓ | ⏸ 待 Whisper 模型 + 测试集 |
| 参数量 / 模型大小 | 复杂度 | ↓ | ✅ 已测 |
| **RTF** | 处理耗时 ÷ 音频时长（CPU 单线程） | ↓ | ✅ 已测 |
| 处理延迟 p50/p95/p99 | 每帧计算耗时分位数 | ↓ | ✅ 已测 |
| 算法延迟 | STFT 窗长决定的固有延迟 | ↓ | ✅ 32 ms（固定） |

### 为什么 CER 是第一指标，而不是 SI-SDR

本地实验已经证明**语音质量指标与可懂度经常方向相反**（[`FINDINGS.md`](FINDINGS.md) F-01）：
维纳滤波和 MMSE-LSA 把 SI-SDR 提高了 1~5 dB，但 STOI **普遍下降**。

所以"SI-SDR 涨了 5 dB"这句话对 ASR 是不是好事，**必须直接测 CER 才知道**。
指标表专门设 ΔSI-SDR 与 ΔCER 两列并列，用来展示两者的相关性有多弱 ——
这比单报一个好看的 CER 更有说服力。

### 为什么判断实时性要看 p99 而不是 RTF 均值

RTF 均值只有 1 ms、但 p99 冲到 20 ms 的系统，听感上是每隔几秒爆一次音。
**帧预算是 16 ms**（一个帧移），p99 越过它，缓冲就会持续累积、延迟无界增长。
所以 `LatencyStats.realtime_ok` 的判据是 p99 < 帧预算，不是 RTF < 1。

---

## 2. 已实测：复杂度与实时性

**测量条件**：Intel i7-14700，**CPU 单线程**（`intra_op_num_threads=1`），
16 kHz / n_fft 512 / hop 256，2026-08-05

### 2.1 模型复杂度

| 模型 | 参数量 | ONNX 大小 | 编码器通道 | GRU | 时间感受野 |
|---|---|---|---|---|---|
| crn-nano | 118,370 | 475 KB | 8-16-16-24-32 | 1×64 | 6 帧 |
| crn-lite | 569,282 | 2,237 KB | 16-32-32-48-64 | 2×128 | 6 帧 |
| crn-large | 1,770,722 | — | 24-48-64-64-96 | 2×256 | 6 帧 |

频率维逐层：257 → 129 → 65 → 33 → 17 → 9。三档模型**全部严格因果**（已由测试强制）。

### 2.2 单帧处理耗时 · PyTorch vs ONNX Runtime

| 模型 | PyTorch (ms) | **ONNX (ms)** | 加速比 | ONNX RTF |
|---|---|---|---|---|
| crn-nano | 0.520 | **0.104** | 5.0× | 0.0065 |
| crn-lite | 0.666 | **0.382** | 1.7× | 0.0239 |
| crn-large | 0.861 | 待测 | — | — |

**ONNX Runtime 明显快于 PyTorch**，小模型上尤其显著（5 倍）——
PyTorch 每层的 Python 调度开销在单帧粒度下占比很高，而 ONNX 图做了算子融合和内存复用。
这直接支持了"导出 ONNX 而不是直接部署 PyTorch"这个决定。

### 2.3 完整管线延迟（VAD + 增强 + STFT/iSTFT）

| 方法 | RTF | p50 (ms) | p95 (ms) | p99 (ms) | 帧预算 | 余量 |
|---|---|---|---|---|---|---|
| specsub | 0.0064 | — | — | 0.53 | 16 ms | 30× |
| wiener | 0.0058 | — | — | 0.42 | 16 ms | 38× |
| mmse-lsa | 0.0103 | — | — | 0.55 | 16 ms | 29× |
| crn-nano (ONNX) | 0.0226 | 0.331 | 0.498 | 0.627 | 16 ms | 26× |
| crn-lite (ONNX) | 0.0344 | 0.516 | 0.753 | 1.024 | 16 ms | 16× |

**全部方法的实时性都不是瓶颈** —— 即便最重的 crn-lite 也有 16 倍余量。
这说明在本项目的规模下，**质量才是唯一需要权衡的维度**，不必为了算力牺牲效果。

MMSE-LSA 比维纳慢 60%，代价来自 `scipy.special.exp1`（指数积分）——
这是它用算力换听感的地方，如实体现在表里。

### 2.4 延迟构成

| 组成 | 值 | 能否消除 |
|---|---|---|
| **算法延迟**（STFT 窗长） | **32 ms** | ❌ 方法固有，CPU 再快也消不掉 |
| 处理延迟 p99 | 0.4 ~ 1.0 ms | ✅ 可优化，但目前完全不是瓶颈 |
| 帧移（缓冲粒度） | 16 ms | 可调，但会改变频率分辨率 |
| WebSocket 往返 + 浏览器缓冲 | 待测 | 仅 Web 演示路径有 |

---

## 3. 已实测：DSP 方法质量对比

**测量条件**：8 s 中文新闻语音，合成噪声，无混响，2026-08-05
（合成噪声的统计特性完全已知，便于交叉验证；真实噪声库的结果待 Colab）

| 噪声 | SNR | 方法 | ΔSI-SDR | ΔSTOI | SegSNR |
|---|---|---|---|---|---|
| white | 0 dB | specsub | **+5.55** | +0.036 | 3.35 |
| white | 0 dB | wiener | +4.93 | −0.012 | 3.56 |
| white | 0 dB | mmse-lsa | +4.91 | −0.009 | 3.49 |
| babble | 0 dB | specsub | +1.42 | +0.020 | 3.04 |
| babble | 0 dB | wiener | +2.30 | −0.010 | 3.01 |
| babble | 0 dB | mmse-lsa | +2.30 | −0.010 | 3.00 |
| babble | 5 dB | mmse-lsa | +1.26 | −0.026 | 5.16 |
| **keyboard** | 0 dB | specsub | **−0.59** | −0.033 | 7.57 |
| **keyboard** | 0 dB | wiener | **−0.02** | −0.028 | 5.80 |
| **keyboard** | 5 dB | specsub | **−0.70** | −0.027 | 8.98 |

三个值得注意的现象，详见 [`FINDINGS.md`](FINDINGS.md)：

- **F-01**：SI-SDR 涨、STOI 跌 —— 单通道降噪的经典结论
- **F-02**：冲激噪声把 DSP 收益压到 +0.6 dB 量级（白噪声是 +5 dB）—— 神经网络的立足点
  （⚠️ 早期"全线失效/负收益"的说法已被推翻，见 F-02 的修正说明）
- **F-03**：谱减法 SI-SDR 最高，但那是音乐噪声换来的假象

---

## 4. 主表：完整测试集实测（2026-08-07，**已用修复后测试集 + 真实 CER 更新**）

**数据来源**：`uv run rtse-eval`，本地全量重跑，[`results/local_metrics_new_testset.json`](../results/local_metrics_new_testset.json)。
测试集是 I-21 修复后重新生成的那份（自然时长，780 样本，[`ISSUES.md`](ISSUES.md) I-21 已端到端验证）。

- **SI-SDR / STOI / ESTOI**：全量 780 样本
- **CER**：分层抽样 78 样本（39 格 × 2 条，覆盖每种 SNR/噪声/T60 组合），
  faster-whisper `small` 模型。样本量比客观指标小得多——ASR 一条要几秒，
  780 条全量转写 8 遍要一个多小时，先用抽样拿到方向性结论
- **PESQ / DNSMOS 仍是空的**：PESQ 本地装不上（[`ISSUES.md`](ISSUES.md) I-05），
  只能在 Colab 侧补，而且要用**这份新测试集**重新跑（旧数字是在已知有截断问题的
  测试集上测的，已作废）；DNSMOS 模型文件在但打分推理函数没写（`registry.py`
  只有一个文件存在性探针）

### 4.1 主表

| 方法 | SI-SDR | ΔSI-SDR | STOI | ESTOI | **CER** | **ΔCER** | PESQ | DNSMOS | 参数量 | RTF | p99 |
|---|---|---|---|---|---|---|---|---|---|---|---|
| clean（干净上界） | — | — | — | — | **0.155** | — | — | — | — | — | — |
| noisy（未处理） | 2.51 | — | 0.734 | 0.624 | **0.333** | — | 待补 | 待实现 | — | — | — |
| specsub | 4.66 | +2.16 | 0.735 | 0.624 | **0.352** | **+0.019** | 待补 | 待实现 | 0 | 0.0064 | 0.53 |
| wiener | 4.78 | +2.28 | 0.714 | 0.594 | **0.376** | **+0.043** | 待补 | 待实现 | 0 | 0.0058 | 0.42 |
| mmse-lsa | 4.78 | +2.28 | 0.716 | 0.598 | **0.372** | **+0.039** | 待补 | 待实现 | 0 | 0.0103 | 0.55 |
| crn-nano | 4.30 | +1.80 | 0.740 | 0.623 | **0.394** | **+0.061** | 待补 | 待实现 | 118 K | 0.0102 | 0.31 |
| crn-lite | 5.97 | +3.47 | 0.749 | 0.634 | **0.384** | **+0.051** | 待补 | 待实现 | 569 K | 0.0157 | 0.48 |
| crn-large | 6.46 | +3.95 | 0.767 | 0.660 | **0.381** | **+0.048** | 待补 | 待实现 | 1.77 M | 0.0246 | 0.64 |

**⚠️ 全部 6 种方法的 CER 都比不处理更差** —— 详见下面 F-08，这是本项目
"一句话目标"（降噪对 ASR 到底有没有用）第一次有真实数据支撑的答案，
而答案跟直觉相反。RTF/p99 是用真实训练权重在本地测的。

### 4.2 按维度拆开看（DSP 已用 I-20 修复后的数字，NN 全量）

**按噪声类型**（T60=0.3，ΔSI-SDR vs none）：

| 噪声 | specsub | wiener | mmse-lsa | crn-nano | crn-lite | crn-large |
|---|---|---|---|---|---|---|
| white | +4.54 | +5.08 | +5.00 | +3.20 | **+7.88** | +6.07 |
| hum | +6.00 | +5.87 | +5.94 | +3.17 | +5.31 | +6.00 |
| cafeteria | +1.17 | +1.09 | +1.08 | +0.83 | +2.68 | +3.35 |
| babble | +0.99 | +1.01 | +1.01 | +0.51 | +2.82 | +3.11 |
| car | +1.28 | +0.88 | +0.82 | +0.79 | +1.38 | +1.81 |
| keyboard | −0.01 | +0.59 | +0.61 | +1.22 | +0.47 | +1.96 |

详细解读见 [`FINDINGS.md`](FINDINGS.md) F-05：DSP 只在平稳噪声（white/hum）上有明显优势，
神经网络的优势集中在非平稳噪声（babble/cafeteria/keyboard）——keyboard 修复 I-20 后
从"全员负分"变成"约等于没帮上忙"，但仍是 DSP 表现最差的一档。

**按混响 T60**（SNR=5, noise=babble；⚠️ 这张表还是 I-20 修复前的 DSP 数字，未重新计算——
SNR=5 明显低于 bug 触发的高 SNR 区间，且这里 −3~−4.65 dB 的量级远大于修复带来的
~0.5~1 dB 级别改善，预期结论不受影响，但数字本身还没有验证过）：

| T60 | none | specsub | wiener | mmse-lsa | crn-nano | crn-lite | crn-large |
|---|---|---|---|---|---|---|---|
| 0.0 | 5.29 | 9.09 | 9.46 | 9.33 | 10.67 | 11.20 | **13.93** |
| 0.3 | 4.66 | 7.20 | 7.75 | 7.68 | 8.76 | 9.03 | 9.06 |
| 0.6 | **−3.07** | −4.10 | −4.65 | −4.33 | −4.60 | −3.48 | −3.13 |
| 0.9 | 4.95 | 7.17 | 7.88 | 7.92 | 6.15 | 5.84 | 5.61 |

T60≥0.6 时**所有方法**（含不处理）都是负 SI-SDR，处理还比不处理更差——
去混响模块（计划中的单通道 WPE）至今未实现，见 [`FINDINGS.md`](FINDINGS.md) F-06。

> ⚠️ **这张表的 T60 标称值不等于实际混响时间，结论已作废**（[`ISSUES.md`](ISSUES.md) I-22）。
> `make_rir()` 的镜像阶数曾被写死成 12，最远只能建到 0.56 秒的反射声，
> 实测标称 0.3/0.6/0.9 的**实际** T60 是 **0.34 / 0.58 / 0.63**——后两档几乎相同。
> 函数已修复，但这张表要等测试集重新生成后重跑才有效。
>
> ⚠️ **这张表的混响全部来自合成 RIR**（`make_rir()` 镜像源法），不是真实测量的房间响应。
> 训练时用的是真实 RIR（RIRS_NOISES / DNS IR），评测时用的是合成的——两侧不一致。
> 真实房间的表现是否同样糟糕，目前没有数据支撑。详见 [`FINDINGS.md`](FINDINGS.md) F-07。

### 4.3 失败案例分析

已确定收录（带具体数据支撑，非猜测）：

1. **谱减法的音乐噪声** —— 频谱图上的孤立残余峰（F-03）
2. **冲激噪声击穿 MCRA** —— 键盘敲击段的噪声估计曲线；修复 I-20 后 keyboard 仍是
   DSP 表现最差的噪声类型，机制依然成立（F-02）
3. **MCRA 高 SNR 崩溃，已修复**——混响 + 高 SNR 组合让噪声估计器的局部最小值
   追踪找不到真实地板，双时间尺度修复后大幅改善；一个更深层的残留问题
   （短促静音间隙场景）确认无法靠调参解决，仍未解决，值得作为失败案例收录
   ——"修复一个 bug 之后，如何验证修复真的有效、而不是仅仅移动了问题"本身
   就是一个有内容的案例（详见 I-20 的完整排查过程）
4. **混响吃掉一切** —— T60≥0.6s 时所有方法（含不处理）SI-SDR 均为负（F-06）
5. 待补：VAD 误切导致丢字对下游 CER 的影响（需要 ASR 接入后才能测）

---

## 5. 测量方法学

为了让数字可信、可复现：

1. **指标一律在内存数组上计算，绝不写盘再读回**。
   `write_audio` 的削波保护会静默改变幅度，污染 SDR、SegSNR 这类非尺度不变指标（I-07）。
2. **RTF 与延迟在 CPU 单线程下测**。多线程能让数字好看，但掩盖真实算力需求，
   也无法反映多路并发时的表现。本机 onnxruntime 恰好只有 CPU provider，正合适。
3. **离线评测走的就是实时路径**。`StreamingEnhancer.process()` 内部逐帧调用
   `process_frame`，与 Web 演示、与 C++ 版是同一条代码路径。
   离线数字**就是**部署时的数字，不存在"论文好、上线崩"。
4. **NN 与 DSP 走完全相同的管线**。`OnnxEnhancer` 包装成 `StreamingEnhancer`，
   同一条 `Pipeline`、同样的 STFT、同样的计时点 —— 对比才是公平的。
5. **看分位数不看均值**。实时系统的失败由尾部决定。
